- install_x_dependencies:
  - exec_in: apt-get update
  - exec_in: apt-get install libxext-dev libxt-dev libxi-dev libxmu-dev
- install_mesa_apt:
  # TOFIX is update needed?
  #- exec_in: apt-get update
  #- exec_in: apt-get install libegl1-mesa-dev
  #- exec_in: apt-get install freeglut3-dev
  #- exec_in: apt-get install libxmu-dev libxi-dev --assume-yes
  - exec_in: apt-get install freeglut3-dev libegl1-mesa-dev --assume-yes

