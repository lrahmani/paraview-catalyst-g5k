# install HDF5
tar -xzvf /home/lrahmani/Software/hdf5-1.8.16.tar.gz
cd hdf5-1.8.16/
cd .. && mkdir hdf5-1.8.16-build && cd hdf5-1.8.16-build/
../hdf5-1.8.16/configure --prefix=/opt/ --exec-prefix=/opt/ --with-zlib --enable-cxx
make -j24
make -j24 install
# add /opt/ to LIB and INC flags 
apt-get install h5tools


# install HDF5
tar -xzvf /home/lrahmani/Software/hdf5-1.8.16.tar.gz
cd hdf5-1.8.16/
cd .. && mkdir hdf5-1.8.16-build && cd hdf5-1.8.16-build/
../hdf5-1.8.16/configure --prefix=/opt/ --exec-prefix=/opt/ --with-zlib --enable-cxx
make -j24
make -j24 install
# add /opt/ to LIB and INC flags 

